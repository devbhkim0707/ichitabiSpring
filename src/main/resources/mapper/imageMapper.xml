<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ichiban.ichitabi.common.image.ImageMapper">

    <!--  이미지 insert  -->
    <insert id="insertImage" parameterType="imageDto" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT  INTO    image(owner_id, type, img_url, rep_img_yn)
        VALUES          (#{ownerId}, #{type}, #{imgUrl}, #{repImgYn})
    </insert>
</mapper>