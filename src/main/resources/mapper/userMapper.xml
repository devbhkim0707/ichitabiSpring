<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ichiban.ichitabi.user.mapper.UserMapper">

    <!--  회원 가입  -->
    <insert id="userSignup" parameterType="userDto" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT  INTO user(email, pw, nickname, birthday, gender, type)
        VALUES (#{email}, #{pw}, #{nickname}, #{birthday}, #{gender}, #{type});
    </insert>

    <!--  로그인  -->
    <select id="userLogin" parameterType="String" resultType="userDto">
        SELECT  id, email, pw, nickname, birthday, gender, type
        FROM    user
        WHERE   email = #{email}
            AND deleted_at IS NULL
    </select>

    <select id="findByEmail" parameterType="String">
        SELECT  *
        FROM    user
        WHERE   email = #{email}
        AND     deleted_at IS NULL
    </select>
</mapper>